// Retired deck-splitting command.
//
// The frozen monolith predates later hand-maintained components, routing, and
// metadata. Re-splitting it would delete current deck work. Keep the monolith as
// provenance and use deck:diff only as a historical comparison.

throw new Error(
  "tools/split_deck.cjs is retired because the frozen monolith is no longer a complete deck source.",
);
